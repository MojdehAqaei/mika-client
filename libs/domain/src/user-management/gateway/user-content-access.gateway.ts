import { UserContentAccessModel } from '../model/user-content-access.model';
import { Gateway } from '../../gateway';

export abstract class UserContentAccessGateway extends Gateway<UserContentAccessModel>{
}
