import { FitToContentDirective } from './fit-to-content.directive';

describe('FitToContentDirective', () => {
  it('should create an instance', () => {
    const directive = new FitToContentDirective();
    expect(directive).toBeTruthy();
  });
});
